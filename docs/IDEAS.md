# Ideas

Отложенные идеи и улучшения, всплывающие по ходу работы. Не вставляются в текущую
итерацию — обсуждаются перед стартом следующей.

## Iteration 2 (после шага 16)

- **User menu в MainLayout.** При клике на иконку пользователя в хедере — Material
  `mat-menu`: для гостя «Sign in / Register», для авторизованного — email + «Sign
  out». На logout вызывать `AuthStore.logout()` и редиректить на `/menu`.
  Зачем: сейчас нет UI-способа разлогиниться, тестирование auth-flow требует
  ручной очистки cookie/localStorage.
